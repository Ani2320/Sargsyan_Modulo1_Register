import '../styles/header.css'

function Header() {
  return (
    <header>
        <div className = "navigator"></div>
        <div class = "ml-20">
            <h2 id = "screen-name">Inicio</h2>
        </div>
    </header>
  )
}

export default Header